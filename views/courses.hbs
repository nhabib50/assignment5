<div class="row">
  <div class="col-md-12">
    <br />
    <h2>Courses</h2>
    <hr />
      <div class="table-responsive">
        <table class="table table-dark">
          <thead>
            <tr>
              <th scope="col">Course Code</th>
              <th scope="col">Course Description</th>
            </tr>
          </thead>
          <tbody>
            {{#each courses as |course|}}
              <tr>
                <th scope="row"><a href="/course/{{course.courseId}}">{{course.courseId}}:{{course.courseCode}}</a></th>
                <th scope="row"><a href="/students?course={{course.courseId}}">{{course.courseDescription}}</a></th>
              </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
  </div>
  {{#unless courses}}
    <div class="col-md-12 text-center">
      <strong>{{message}}</strong>
    </div>
  {{/unless}}
</div>